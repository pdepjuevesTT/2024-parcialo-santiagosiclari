import solucion1.*

describe "Describe1" {
  const debito1 = new Debito()
  const juan = new Persona(dineroEfectivo = 500,dineroDebito = 400,metodoPreferido = debito1, metodos = [efectivo, debito1])
  const pancho = new Persona(metodoPreferido = debito1)
  test "Test1" {
    assert.that(pancho.metodoPreferido().puedoPagar(juan, 300))

  }
}